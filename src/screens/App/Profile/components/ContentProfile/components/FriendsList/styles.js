import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  ListContainer: {
    flex: 1,
  },
  ProfileView: {
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: '#A781D6',
    width: 60,
    height: 60,
    borderRadius: 30,
  },
});

export default styles;
